import { EuiIcon } from '@elastic/eui';
import styled from 'styled-components';

export const DashboardContainer = styled.div`
    max-width: 1000px;
    width: 100%;
    margin: 0 auto;
`;

export const StatIcon = styled(EuiIcon)`
    margin-right: 5px;
`;